# Virtual environments (don't commit these)
venv/
env/

# Node.js dependencies (don't commit these)
node_modules/

# Environment variables (don't commit these)
.env

# Database files (don't commit these)
*.db
*.sqlite

# Logs (don't commit these)
logs/
*.log

# Python cache (don't commit these)
__pycache__/
*.pyc

# Keep ABI files - they are essential for the app
!artifacts/
!artifacts/trex/
